server:
  port: 8888

spring:
  application:
    name: config-server
  rabbitmq:
    host: localhost
    port: 5672
    username: vagrant
    password: vagrant
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hexaforce/config-repo.git

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
