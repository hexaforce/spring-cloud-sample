spring:
  application:
    name: turbine-stream
  rabbitmq:
    host: localhost
    port: 5672
    username: vagrant
    password: vagrant
    
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
