info:
  component: Zuul Server
server:
  port: 8765
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  session:
    store-type: redis
  security:
    user:
      password: password
#    ignored: "*.bundle.*"
zuul:
  routes:
    resource:
      path: /resource/**
      url: http://localhost:8765
      sensitive-headers:
    